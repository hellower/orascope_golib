package main

import (
	"fmt"
	sbs "github.com/hellower/orascope_golib/orascopeUtils"
)

func main() {
	var x string = "똠방각하."
	y := sbs.StringToByteSlice(x)
	//	y := []byte(x)
	z := sbs.ByteSliceToString(y)

	fmt.Println(y, z)
}
